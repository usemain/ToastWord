const emptyFn = () => {
}

const SPACING = 20
const ITEM_SCALE = (SPACING * 2) + 70

const pronunciationApi = 'https://dict.youdao.com/dictvoice?audio='

export {
  emptyFn, SPACING, ITEM_SCALE, pronunciationApi
}
